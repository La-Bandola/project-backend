from django.apps import AppConfig


class AhorrosConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name               = 'apps.ahorros'
    verbose_name       = 'Ahorros'
